#include <stdio.h>

int main(int argc, char **argv) {
	int userAge = 5;
	userAge = userAge + 4;
	char *userName = "Dima";
	float k = 1.885;
	
	printf("user age: %d, user name: %s, k: %f\n", userAge, userName, k);
}
